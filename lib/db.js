import mongoose from "mongoose";
export default function connection() { return mongoose.connection.readyState === 0 && mongoose.connect(`mongodb+srv://${process.env.MONGO_USER}:${process.env.MONGO_PASS}@vebinvoice.2asyckb.mongodb.net/server`) }